@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager


@if(SignInManager.IsSignedIn(User))
{
    Layout = "Layouts/_LayoutExamService";
}
else
{
    Layout = "Layouts/_LayoutMain";
}